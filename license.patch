--- license.ts
+++ license.ts
@@ -108,6 +108,16 @@
 				saveCertStr,
 				deviceFingerprint: () => this.instanceSettings.instanceId,
 			});

 			// === HACK: forçar "Enterprise" em modo local ===
 			if (process.env.N8N_SKIP_LICENSE_CHECK === 'true') {
 				this.logger.warn('⚠️ Pulando checagem de licença (N8N_SKIP_LICENSE_CHECK=true)');
 				// habilita todas as features
 				this.manager.hasFeatureEnabled = () => true;
 				// aumenta cotas ilimitadas (exemplo para time-project limit)
 				this.manager.getFeatureValue = (_feature) => Infinity;
 				// força plano enterprise
 				this.manager.getMainPlan = () => ({ productId: 'enterprise', planName: 'enterprise' } as any);
 			}

 			await this.manager.reload();
 			this.logger.debug('License reloaded');
